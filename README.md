# 🔬 نظام إدارة المختبرات الطبية (Al-Mokhtabar Pro)

نظام متكامل لإدارة مختبرات التحاليل الطبية، مبني باستخدام React و Electron ليعمل كتطبيق سطح مكتب (Windows EXE). يتميز النظام بدعم العمل بدون إنترنت (Offline First) مع ميزات ذكاء اصطناعي لتحليل النتائج عند توفر الاتصال.

## ✨ المميزات الرئيسية

*   **إدارة المرضى**: تسجيل بيانات المرضى، البحث، وتعديل المعلومات.
*   **إدارة الزيارات**: إنشاء زيارات جديدة، اختيار الفحوصات، وحساب التكلفة تلقائياً.
*   **إدخال النتائج**: واجهة سهلة لإدخال نتائج الفحوصات ومقارنتها بالمعدلات الطبيعية.
*   **دليل الفحوصات**: إدارة قائمة الفحوصات، الأسعار، والوحدات.
*   **التقارير**: طباعة تقارير طبية احترافية بضغطة زر.
*   **الذكاء الاصطناعي**: تحليل النتائج الطبية باستخدام Google Gemini (يتطلب إنترنت).
*   **لوحة تحكم**: إحصائيات مالية وعددية للعمل اليومي.

## 🚀 طريقة التشغيل (للمطورين)

1.  تثبيت الحزم:
    ```bash
    npm install
    ```
2.  تشغيل وضع التطوير (Web):
    ```bash
    npm run dev
    ```
3.  تشغيل نسخة سطح المكتب (Electron Dev):
    ```bash
    npm run electron
    ```
4.  بناء ملف التنفيذ (EXE):
    ```bash
    npm run dist
    ```

## 🛠 التقنيات المستخدمة

*   **Frontend**: React, TypeScript, TailwindCSS
*   **Desktop Wrapper**: Electron
*   **Build Tool**: Vite
*   **Icons**: Lucide React
*   **Charts**: Recharts
*   **AI**: Google Gemini API

## 📦 التحميل

يمكنك تحميل أحدث نسخة من التطبيق (ملف exe) من قسم **Actions** ثم اختيار آخر عملية بناء وتحميل الملف من **Artifacts**.